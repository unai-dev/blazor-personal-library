@typeparam T


@foreach (var element in List)
{
    @ChildContent(element);
}




@code {

    [Parameter, EditorRequired] public List<T> List { get; set; }
    [Parameter, EditorRequired] public RenderFragment<T> ChildContent { get; set; }

}
